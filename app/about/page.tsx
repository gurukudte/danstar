import About from "./components/About";

export const metadata = {
  alternates: {
    canonical: "https://www.danstar.in/about",
  },
};

export default function AboutPage() {
  return <About />;
}

// Reuse components unchanged from your original
